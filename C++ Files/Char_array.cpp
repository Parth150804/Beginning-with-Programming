#include <iostream>
#include <algorithm>
#include <stack>
using namespace std;


int main() {
// NOTE: for a word of size 'n', we declare a char arr of size 'n+1'.

    // char arr[100] = "apple";     ------> Outputing the char arr
    // int i = 0;
    // while (arr[i] != '\0') {
    //     std::cout << arr[i] << "\n";
    //     i++;
    // }


//      Checking palindrome
    // int n;
    // std::cout << "Enter length of word: ";
    // std::cin >> n;

    // char arr[n+1];              // -------->  Taking input
    // std::cin >> arr;

    // bool check = 1;

    // for (int i= 0; i < n; i++) {
    //     if (arr[i] != arr[n-1-i]) {
    //         check = 0;
    //         break;
    //     }
    // }

    // if (check == true) {
    //     std::cout << "Word is a palindrome";
    // }
    // else {
    //     std::cout << "Word is not a palindrome";
    // }

// Largest word in a sentence;

// int n;
// std::cin >> n;
// cin.ignore();

// char arr[n+1];

// std::cin.getline(arr, n);    // ----------->> it tells the compiler to take words after spaces also in a single input
// std::cin.ignore();

// int i = 0;
// int currlen = 0, maxlen = 0;

// while (1) {
//     if (arr[i] == ' ' or arr[i] == '\0') {
//         if (currlen > maxlen) {
//             maxlen = currlen;
//         }

//         currlen = 0;
        

//     }
//     else
//     currlen++;
//     if (arr[i] == '\0') {
//         break;

//     i++;

//     }

// }

// std::cout << maxlen;



}